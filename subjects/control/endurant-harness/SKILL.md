---
name: endurant-harness
description: Use only when explicitly invoked for the evaluation fixture.
---

# Minimal control

Complete the requested repository task directly. Follow repository instructions,
preserve existing work, make the smallest correct change, run the verification
warranted by the task, and report only evidence actually observed. Do not create
workflow artifacts, use subagents, or add unrelated checks.

