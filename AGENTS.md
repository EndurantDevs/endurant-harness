# Evaluation Lab Instructions

- Keep the installed Endurant Harness and production repositories read-only.
- Keep raw model JSONL, generated workspaces, caches, and full logs under ignored artifact paths.
- Use neutral synthetic identifiers in tracked fixtures, reports, commits, and possible publication artifacts.
- Run focused integrity tests and the combined strict audit before the local preflight.
- Performance claims require paired unchanged/current and candidate measurements; ordinary correctness work skips irrelevant synthetic benchmarks.
- Do not configure a remote, push, open a pull request, or invoke remote CI without explicit authorization.
