{
  "schema_version": 1,
  "cases": [
    {"id":"d01","expected_lane":"direct","tags":["internal","single-package"],"task":"In src/labels.py, fix the private normalize_label helper so an empty suffix is preserved; update its named focused test."},
    {"id":"d02","expected_lane":"direct","tags":["internal","single-package"],"task":"In src/rows.py, correct the internal merge_rows helper for an empty list; the exact unit test path is tests/test_rows.py."},
    {"id":"d03","expected_lane":"direct","tags":["internal","reversible"],"task":"Change the private token splitter in src/tokenize.py to retain a trailing delimiter and add one focused regression."},
    {"id":"d04","expected_lane":"direct","tags":["internal","known-proof"],"task":"Fix the internal display-name formatter in src/names.py for whitespace-only aliases; run tests/test_names.py."},
    {"id":"d05","expected_lane":"direct","tags":["internal","single-package"],"task":"Correct the package-private clamp_index helper in src/indexing.py for negative zero and add its focused test."},
    {"id":"d06","expected_lane":"direct","tags":["internal","reversible"],"task":"In src/flags.py, preserve explicit false in the private merge_flags helper; update tests/test_flags.py."},
    {"id":"d07","expected_lane":"direct","tags":["internal","known-proof"],"task":"Fix an off-by-one in the internal chunk_names helper at src/chunks.py; the focused test is tests/test_chunks.py."},
    {"id":"d08","expected_lane":"direct","tags":["internal","single-package"],"task":"Make the private parse_hint helper in src/hints.py ignore one leading space; add a focused regression."},
    {"id":"d09","expected_lane":"direct","tags":["internal","reversible"],"task":"Correct the internal stable_unique helper in src/unique.py for repeated empty strings; run its named unit test."},
    {"id":"d10","expected_lane":"direct","tags":["internal","known-proof"],"task":"Fix the private coalesce_note helper in src/notes.py so zero is not treated as missing; update tests/test_notes.py."},
    {"id":"d11","expected_lane":"direct","tags":["internal","single-package"],"task":"In src/paths.py, correct the internal trim_segment helper for a root segment and run tests/test_paths.py."},
    {"id":"d12","expected_lane":"direct","tags":["internal","reversible"],"task":"Fix the private render_cell helper in src/table.py for an empty value; add one focused test."},
    {"id":"d13","expected_lane":"direct","tags":["internal","known-proof"],"task":"Change the internal pick_default helper in src/defaults.py to preserve integer zero; update the exact focused test."},
    {"id":"d14","expected_lane":"direct","tags":["internal","single-package"],"task":"Correct the private flatten_once helper in src/flatten.py for an empty nested list; run tests/test_flatten.py."},
    {"id":"d15","expected_lane":"direct","tags":["internal","reversible"],"task":"Fix the internal quote_word helper in src/quotes.py for a single apostrophe and add a regression."},
    {"id":"d16","expected_lane":"direct","tags":["internal","known-proof"],"task":"In src/order.py, keep equal items stable in the private order_items helper; run tests/test_order.py."},
    {"id":"d17","expected_lane":"direct","tags":["internal","single-package"],"task":"Correct the private decode_marker helper in src/markers.py for a blank marker; update its focused test."},
    {"id":"d18","expected_lane":"direct","tags":["internal","reversible"],"task":"Fix the internal pair_values helper in src/pairs.py for one missing tail value; add one unit regression."},
    {"id":"d19","expected_lane":"direct","tags":["internal","known-proof"],"task":"Make the private suffix_key helper in src/keys.py preserve a zero suffix; run tests/test_keys.py."},
    {"id":"d20","expected_lane":"direct","tags":["internal","single-package"],"task":"Fix the internal compact_spaces helper in src/spaces.py for a newline-only value; update the named test."},

    {"id":"e01","expected_lane":"escalated","tags":["performance"],"task":"Optimize select_records and claim at least a 40 percent p95 speed improvement without changing semantics."},
    {"id":"e02","expected_lane":"escalated","tags":["performance"],"task":"Reduce parser memory usage under a production-like million-row workload and report the improvement."},
    {"id":"e03","expected_lane":"escalated","tags":["migration"],"task":"Add a database migration that rewrites stored status values while old application instances remain live."},
    {"id":"e04","expected_lane":"escalated","tags":["persisted-data"],"task":"Change the on-disk checkpoint schema and retain compatibility with checkpoints written by the prior release."},
    {"id":"e05","expected_lane":"escalated","tags":["security"],"task":"Change authorization so delegated users can read a new class of records."},
    {"id":"e06","expected_lane":"escalated","tags":["auth"],"task":"Rotate the signing algorithm used for session tokens and preserve rolling-upgrade compatibility."},
    {"id":"e07","expected_lane":"escalated","tags":["deployment"],"task":"Modify the production deployment manifest and readiness probe for the API pods."},
    {"id":"e08","expected_lane":"escalated","tags":["shared-config"],"task":"Change a shared feature flag default consumed by six independently deployed services."},
    {"id":"e09","expected_lane":"escalated","tags":["concurrency"],"task":"Fix an intermittent race in the worker cancellation path that appears only under parallel load."},
    {"id":"e10","expected_lane":"escalated","tags":["flaky"],"task":"Repair a flaky integration test whose failure alternates between timeout and duplicate delivery."},
    {"id":"e11","expected_lane":"escalated","tags":["cross-package"],"task":"Change a shared utility used by the api, importer, and worker packages and update all affected callers."},
    {"id":"e12","expected_lane":"escalated","tags":["coupling"],"task":"Refactor a message type whose implementation and consumers live in three packages."},
    {"id":"e13","expected_lane":"escalated","tags":["contradiction"],"task":"The named focused test says null should be rejected, but the public documentation says null must use a default; implement the fix."},
    {"id":"e14","expected_lane":"escalated","tags":["unknown-proof"],"task":"Fix an internal cache bug, but no focused test or local reproduction is known and two implementations exist."},
    {"id":"e15","expected_lane":"escalated","tags":["public-api"],"task":"Rename a public SDK method while keeping downstream clients source compatible."},
    {"id":"e16","expected_lane":"escalated","tags":["external-protocol"],"task":"Change the JSON wire representation exchanged with an external partner."},
    {"id":"e17","expected_lane":"escalated","tags":["dependency"],"task":"Upgrade the primary framework and regenerate its lockfile after a breaking security release."},
    {"id":"e18","expected_lane":"escalated","tags":["generated-client"],"task":"Update an API schema and regenerate clients consumed by two packages."},
    {"id":"e19","expected_lane":"escalated","tags":["platform"],"task":"Change native packaging and codesigning behavior for both arm64 and x86_64 releases."},
    {"id":"e20","expected_lane":"escalated","tags":["destructive"],"task":"Remove obsolete stored artifacts from every workspace during startup."}
  ]
}
